# Original research archive

Source files are copied unchanged from the originating jev-reasoning workspace.
This is a provenance snapshot, not the entry point for running this package.
Historical R1 scripts depend on preceding R0/ProofWriter artifacts, which are not
all bundled. Use `scripts/run_showcase.py` at the project root for standalone
examples. Included R1 and counting JSONL records retain exact prompts and traces.
M1 reports preserve the earlier negative deep-reasoning result; the full original
ProofWriter datasets/predictions and model weights are not redistributed here.
Local planning notes and remote job-management scripts are omitted. Public
method and reproduction documentation is under `docs/`.
